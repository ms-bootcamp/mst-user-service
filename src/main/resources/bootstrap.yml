spring:
  profiles:
    active: ${ACTIVE_PROFILE:local}
  application:
    name: mst-user-service
  cloud:
    consul:
      enabled: true
      hostname: localhost
      port: 8500
      ribbon:
        enabled: true
      discovery:
        enabled: true
        register: true
    config:
#      uri: http://127.0.0.1:6677
      discovery:
        enabled: true
        serviceId: application

management:
  security:
    enabled: false